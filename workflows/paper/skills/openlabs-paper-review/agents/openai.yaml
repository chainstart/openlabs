interface:
  display_name: "OpenLabs Three-Agent Paper Review"
  short_description: "Parallel three-reviewer median quality gate"
  default_prompt: "Use $openlabs-paper-review to run three independent parallel manuscript reviews and validate their median panel result."
policy:
  allow_implicit_invocation: false
