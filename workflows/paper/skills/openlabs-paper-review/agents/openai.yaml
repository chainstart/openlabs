interface:
  display_name: "OpenLabs Independent Paper Review"
  short_description: "Fresh Codex review with optional Claude panel"
  default_prompt: "Use $openlabs-paper-review to run the configured fresh Codex review and validate its panel result, adding blind Claude review only when enabled."
policy:
  allow_implicit_invocation: false
