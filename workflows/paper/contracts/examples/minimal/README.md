# Minimal result bundle

This fixture shows the smallest accepted bundle shape. Validate it with:

```bash
python -m paper_writing bundle contracts/examples/minimal
```

The command prints the producer commit and manifest SHA-256 needed by a paper's
`evidence_bundles` registry entry.
