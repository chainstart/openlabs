# Episode 1 plan

- [x] Restore `lake build`; the current checkout no longer contained the
  duplicate `WowiiConjecture13.lean` declarations reported by the prior host
  observation.
- [x] Repair `WowiiConjecture58.lean` finset partition/cardinality lemmas
  currently failing in Lean.
- [x] Add the exact structural upper bound and equality
  `graph.largestInducedForestSize = 5`.
- [x] Add the exact bipartite invariant equality `graph.b = 6`.

Note: the requested run directory was not writable in this sandbox, so this
checkpoint is recorded in the Lean workspace.
